# Optional Hugging Face token for faster model downloads
HF_TOKEN=

# Default environment mode override
DD_DATA__MODE=local_only

# Local data path
DD_DATA__LOCAL_DATA_DIR=drug_discovery_env/data

# Runtime logging directory
DD_APP__LOG_DIR=artifacts/logs
